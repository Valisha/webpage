---
interface Props {
  artists: string[];
}

const { artists } = Astro.props;
---

{
  artists.map((artist, index) => {
    return (
      <>
        {artist}
        <span>{index === artists.length - 1 ? ' ' : ', '}</span>
      </>
    );
  })
}
